import { Screen, EmptyState } from "@/components";

export default function ProjectDetailStub() {
  return (
    <Screen style={{ justifyContent: "center" }}>
      <EmptyState glyph="▤" title="Project details" description="Meetings, members, and settings for this project — coming in Phase 5." />
    </Screen>
  );
}
